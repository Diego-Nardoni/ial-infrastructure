#!/bin/bash
# Windows build script - requires Windows environment or Wine
# Run this on Windows with Python and PyInstaller installed:
# pyinstaller build/ialctl_fixed.spec --clean --noconfirm --distpath dist/windows
echo 'Windows binary requires Windows environment to build'
echo 'Use this spec file: build/ialctl_fixed.spec'

